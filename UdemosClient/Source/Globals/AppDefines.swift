//
//  AppDefines.swift
//  UdemosClient
//
//  Created by Sinisa Abramovic on 25.03.2023..
//

import Foundation

enum AppDefines {
    
    // MARK: - Defines for Visuals -
    
    enum Visuals {
        
        enum MessangerView {
            static let topPaddingOffest = 0.15
        }
        
        enum CornerRadius {
            static let defaultCornerRadius = CGFloat(8)
            static let defaultHalfCornerRadius = CGFloat(4)
        }
        
        enum Padding {
            static let defaultPadding = CGFloat(8)
            static let smallPadding = CGFloat(4)
        }
        
        enum Spacing {
            static let smallSpacing = CGFloat(8)
            static let mediumSpacing = CGFloat(8)
        }
    }
    
}
